#Range function in python

numbers= list(range(10))
print(numbers)

numbers= list(range(1,10)) #the first element is inclusive while the second is exclusive
print(numbers)

numbers= list(range(1,30,3)) #in this way we can define the interval or steps to the range
print(numbers)
